# Read me

```
mkdir bin && cd bin

# Invoke CMake (only required once)
cmake -G "Visual Studio 17 2022" -A x64 -S .. -B "build64"

# Build
cmake --build build64 --config Debug

# Run
build64\Debug\TicTacToe.exe
```
